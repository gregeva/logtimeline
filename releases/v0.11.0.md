# Release v0.11.0

## What's New

- Enhanced user-defined metrics (`-udm`) with custom regex patterns, automatic unit detection, and unit conversion (#22)
- Add `--help` with structured, categorized help display and usage examples (#30)

## Bug Fixes

- Honor explicit `-hgh` histogram height setting and increase default dynamic heights (#38)
- Fix uninitialized value warning in rolling window calculation when heatmap enabled (#37)
