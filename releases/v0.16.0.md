# Release v0.16.0

## What's New

- User-defined metrics gain counting aggregations — `count`, `distinct`, `ratio`, `rate`, `drate` — with full string-token support, plus a token-key spec field that separates the extraction token from the column label; the canonical UDM average keyword is now `mean` (`avg` still accepted), so duplicate-name collision headers and CSV columns read `x:mean` instead of `x:avg` (#313)
- Numeric criteria can now highlight instead of filter: `-hdmin`/`-hdmax`, `-hbmin`/`-hbmax`, `-hcmin`/`-hcmax` mark entries inside an inclusive duration/bytes/count band across every highlight surface while keeping the full population in view, composing with the hard filters and with `-h` (AND) (#312)
- Sorting the messages table by a computed statistic (`-so p99`, `-so skewness`, etc.) now computes statistics in two passes (eligible candidates, then displayed keys only), cutting the statistics phase up to 40% and peak message-store memory up to 19% on statistic-sorted runs; messages without a defined value for the sort statistic are listed after the ranked ones ordered by occurrences instead of competing as zero, and two statistic-sort scenarios join the benchmark suite (#303)

## Bug Fixes

- `-dmin`/`-dmax` boundaries are now inclusive, matching `-bmin`/`-bmax`/`-cmin`/`-cmax`: entries exactly at the threshold are kept where they were previously dropped (#312)
- The STATS CSV `occurrences` total is confirmed as all lines in the bucket (plain plus highlighted) and is now locked by a test invariant; a dead exclusion alternative in its accumulation regex that contradicted this was removed (#320)
- Numeric filters now report how many lines they excluded solely for carrying no value for the filtered metric, instead of silently shrinking the analyzed population; the metric-carrying requirement is documented in `--help` and the usage guide (#321)
- An inverted numeric range (minimum above maximum) on any of the twelve filter or highlight range options now warns up front that the range is unsatisfiable, instead of producing an empty result or highlighting nothing with no explanation (#322)
- MESSAGES CSV numeric user-defined-metric columns now honor `--csv-precision` like every other numeric column, instead of emitting raw stored floats with up to 13 decimals (#324)
- Counting-aggregation user-defined metrics no longer emit an uninitialized-value division warning per message during statistics calculation; the CSV harness now fails any scenario whose stderr carries Perl runtime warnings (#326)
- `-hm` and `-hg` metric names now resolve through one shared parser: `-hg` accepts user-defined metric names with their original case (previously lowercased and rejected as unknown), `-hm` accepts built-in names case-insensitively (`-hm DURATION`/`-hm TIME` previously fell through to filename handling), and both options' documentation lists UDM metric selection (#327)
- A CSV file swept into a multi-file input glob no longer crashes a `-udm` run with a fatal timestamp-parse error; CSV rows whose timestamp is neither epoch nor ISO are skipped with a warning naming the file, line, and value (#328)
- MESSAGES CSV rows for messages with no duration data now emit blank duration totals instead of `0`/`0 usec`, so a measured zero total is distinguishable from no data; the CSV harness family rules regroup `impact` with the duration columns and split `std_dev`/`cv` into a dispersion family matching their documented populate thresholds (#330)
- The CSV structural harness now fails hard when a produced CSV carries a column absent from the rules spec, matching the drift engine's all-or-nothing coverage so new output columns cannot ship unspecified (#335)
- Access logs in standard/common format (no request-duration field) no longer display latency statistics, fabricated 0ms percentiles, or the statistics variant of the messages table; format detection now classifies such logs as the common format, and mixed runs of files with and without durations no longer dilute percentiles with zero samples (#345)
- `-mem` now measures the message-statistics counter store, correcting an undercount of reported memory under `-mdm bin` (#346)
- Malformed duration values on corrupt access-log lines (concatenated records) are treated as absent instead of entering calculations and the on-disk index; every test harness now fails on Perl runtime warnings from ltl, and the regression fixtures moved off the corrupt sample log (#341)
- With CSV output active, the STATS CSV statistics columns are now emitted regardless of the statistics panel being hidden; statistics capture and computation run only when at least one output surface consumes them, and `-os`/`--omit-stats` is deprecated in favor of `-od`/`--omit-durations` (producer side) or `-hst`/`--hide-stats` (consumer side), with a runtime deprecation notice (#349)
- Shape-moment (skewness/kurtosis/bimodality) and extended-percentile statistics are now captured, computed, and stored only when a consumer (CSV output or a `-so` statistic sort key) reads them: the statistics phase drops below the v0.14.5 baseline on all benchmarked scenarios and peak memory falls up to 25% on statistic-sorted runs; the new `-V statistics-demand` section reports the per-store demand resolution and shape-pass counters (#305)
- The messages and threadpool summary tables no longer allocate memory proportional to `-n` when selecting the top keys: the top-key slice bound is clamped to the number of keys actually present, removing a transient peak-RSS burst (measured ~158 MB at `-n 2000000`) during the output phase (#362)
- `-mem` now accounts for peak memory coherently: the reported maximum includes the output phase (previously under-reported by up to 34%), breakdown percentages are shares of the reported maximum, an explicit `unattributed` row surfaces memory no tracked structure explains (also emitted in the `-V` benchmark block), the message display-order store is tracked, and a new `-mem debug` operand emits per-phase memory diagnostics on stderr (#356)
- File reading on access logs no longer pays an expensive per-line backtracking failure in the format-detection cascade, removing a ~2x read-phase slowdown on with-duration access logs introduced earlier in this release cycle (#358)
- Enhanced-format access logs (quoted referrer, quoted user-agent, then request duration — JBoss/Jersey style) are now detected as their own format and their durations captured; previously the broader with-duration pattern claimed these lines with no duration, so latency statistics never appeared for them (#365)

## Performance

Single-machine, single-run capture; full per-test results in `tests/baseline/results/comparison-v0.15.1-vs-v0.16.0.md`. Standard selections compare against v0.15.1; the extra-large access-log selections compare against v0.15.0 (the most recent baseline that included them).

The statistics phase is 50–62% faster across all measurable cases, and peak memory improved on most selections (up to −13.2%). Access-log selections show a read-phase timing regression — see Known Issues.

### Timing — selected cases

| Test | baseline | v0.16.0 | Δ |
|------|----------|---------|---|
| humungous-log-uniqueness — `-n 25` (286K unique keys) | 2.9 s | 2.7 s | **−5.8%** |
| humungous-log-uniqueness — `-n 25 -g` | 12.0 s | 10.3 s | **−13.9%** |
| multi-day-custom-logs — standard | 18.0 s | 16.5 s | **−8.1%** |
| multi-day-custom-logs — `-n 25 -g` | 1 min | 52.6 s | **−14.3%** |
| single-day-access-log — standard | 10.7 s | 11.3 s | **+6.1%** |
| month-single-server-access-logs — standard | 2.0 min | 2.1 min | **+8.5%** |
| month-many-servers-access-logs — standard | 9.6 min | 10.7 min | **+11.5%** |
| month-many-servers-access-logs — `-n 25 -g` | 30 min | 30.2 min | **+0.5%** |

### Memory (peak RSS) — selected cases

| Test | baseline | v0.16.0 | Δ |
|------|----------|---------|---|
| single-day-access-log — standard | 165.4 MB | 143.5 MB | **−13.2%** |
| single-day-access-log — `-n 25 -g` | 207.7 MB | 180.4 MB | **−13.2%** |
| multi-day-custom-logs — standard | 204.7 MB | 192.9 MB | **−5.7%** |
| month-single-server-access-logs — standard | 2.7 GB | 2.5 GB | **−4.9%** |
| month-many-servers-access-logs — `-n 25 -g` | 7.3 GB | 6.7 GB | **−8.3%** |

## Known Issues

- Access-log read-phase timing regressed by roughly 4–11.5% across the access-log benchmark selections. Truing up format detection and file-matching in this release (#341, #345, #365) amplified the existing per-line cost of the format-detection cascade on access logs. Planned to be fixed by the core parsing architecture transition to the Log Format Registry (#23). The trade in this release: access logs pay a higher read cost in exchange for a 50–62% faster statistics phase, lower peak memory, and correct duration capture on formats that previously produced no latency statistics.
