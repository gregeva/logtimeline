# Release v0.14.5

## What's New

- Re-running ltl against files it has already analyzed is now faster: prior runs' bound information is read from `ltl-index.csv` at startup, skipping the initial bound-discovery scan (#179)
- Histogram bottom axis (`-hg`) now shows tick marks at each percentile position so legend values line up visually with the distribution (#185)
- New `--percentile-precision` / `-pp` (tier 1–9) and `--percentile-buckets-per-decade` / `-pbpd` flags to tune percentile accuracy; `--exact-percentiles` / `-ep` opt-out for the previous sort-based computation (#189)
- Heatmap (`-hm`) and histogram (`-hg`) use significantly less memory on larger files (see Benchmark Comparison below). Output is visually identical to previous releases. Use `--exact-percentiles` to fall back to the previous behavior (#34)

## Bug Fixes

- (none)

## Benchmark Comparison (v0.14.4 → v0.14.5)

Both baselines now captured on the same machine — issue #213 traced an apparent XL consolidation regression to a cross-machine baseline artifact in the pre-release data; there is no consolidation regression. Timing improves or is flat on every scenario, with the largest wins on heatmap/histogram XL where the bin-counter migration applies.

### Heatmap and Histogram Tests — Memory

| Test | v0.14.4 | v0.14.5 | Delta |
|------|---------|---------|-------|
| single-day-access-log (762K lines, 148 MB) — `-hm` | 160.4 MB | 120.1 MB | **-25%** |
| single-day-access-log — `-hg` | 288.2 MB | 154.6 MB | **-46%** |
| single-day-access-log — `-hm -hg` | 292.1 MB | 120.7 MB | **-59%** |
| single-day-access-log — `-hm -hg -g` | 328.9 MB | 167.1 MB | **-49%** |
| multi-day-custom-logs (1.5M lines, 463 MB) — `-hm` | 206.4 MB | 179.6 MB | **-13%** |
| multi-day-custom-logs — `-hg` | 241.9 MB | 199.2 MB | **-18%** |
| multi-day-custom-logs — `-hm -hg` | 248.2 MB | 177.2 MB | **-29%** |
| multi-day-custom-logs — `-hm -hg -g` | 294.1 MB | 232.4 MB | **-21%** |
| month-single-server-access-logs (~12M lines, 1.5 GB) — `-hm` | 2.7 GB | 2.2 GB | **-22%** |
| month-single-server — `-hg` | 4.1 GB | 2.7 GB | **-35%** |
| month-single-server — `-hm -hg` | 4.2 GB | 2.2 GB | **-49%** |
| month-single-server — `-hm -hg -g` | 3.3 GB | 1.3 GB | **-60%** |
| month-many-servers-access-logs (~39M lines, 7.6 GB) — `-hm` | 12.9 GB | 10.0 GB | **-22%** |
| month-many-servers — `-hm -hg` | 14.5 GB | 10.2 GB | **-30%** |
| month-many-servers — `-hm -hg -g` | 14.3 GB | 4.9 GB | **-66%** |

### Heatmap and Histogram Tests — Timing

| Test | v0.14.4 | v0.14.5 | Delta |
|------|---------|---------|-------|
| single-day-access-log — `-hg` | 11.4 s | 11.3 s | **-1%** |
| single-day-access-log — `-hm -hg` | 12.0 s | 11.8 s | **-2%** |
| single-day-access-log — `-hm -hg -g` | 20.9 s | 16.2 s | **-22%** |
| multi-day-custom-logs — `-hg` | 17.0 s | 16.7 s | **-2%** |
| multi-day-custom-logs — `-hm -hg` | 17.3 s | 17.1 s | **-1%** |
| multi-day-custom-logs — `-hm -hg -g` | 57.0 s | 52.4 s | **-8%** |
| month-single-server-access-logs — `-hm -hg` | 2.7 min | 2.3 min | **-14%** |
| month-single-server — `-hm -hg -g` | 6.5 min | 4.2 min | **-35%** |
| month-many-servers-access-logs — `-hg` | 16.2 min | 11.4 min | **-29%** |
| month-many-servers — `-hm -hg` | 13.9 min | 11.7 min | **-16%** |
| month-many-servers — `-hm -hg -g` | 43.4 min | 28.4 min | **-35%** |

### Known Observations

**Read-phase cost on histogram-bearing XL scenarios:** the bin-counter migration moved per-message histogram/heatmap accumulation into the read loop, so `read_files` grew ~14–19% on XL runs that compute `-hm` or `-hg`. The eliminated post-processing phases save more than the read phase grows, so total wall-time still improves on every affected scenario (see Timing table above). Other improvements already planned in the read path will address this.

**Small-file heatmap/histogram memory:** files under ~100 MB show a 2–7% memory increase (~2 MB absolute). The new histogram backend has a small fixed overhead that's proportionally visible when the file is too small for the savings to dominate. On any file larger than that, the migration is a clear net win — see table above.
